# Core dependencies
flask==2.3.2
flask-cors==3.0.10
pymongo==4.10.1
python-dotenv==1.0.0

# Transitive dependencies for Flask
blinker==1.6.2
itsdangerous==2.1.2
Jinja2==3.1.2
Werkzeug==2.3.7

# Transitive dependencies for flask-cors
Six==1.16.0

# Additional dependencies for Jinja2 and its transitive requirements
MarkupSafe==2.1.2

# Compatibility for Python 3.9 (importlib-metadata and click)
importlib-metadata==3.10.0
click==8.1.3
